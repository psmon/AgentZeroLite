<Window x:Class="AgentZeroWpf.UI.Components.InstallPluginPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Install WebDev Plugin"
        Width="620" Height="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#1B1B1C">
    <Window.Resources>
        <Style x:Key="ChoiceButton" TargetType="Button">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A2A2D"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0A84FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0A84FF"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#9D9D9D"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- ─── Dark ComboBox (full re-template) ───────────────────────
             Default WPF ComboBox falls back to SystemColors for the
             ToggleButton chrome, the Popup background, and ComboBoxItem
             highlight — so plain Background/Foreground setters look broken
             on a dark window. The four pieces below override every
             system-color path.
             See harness/knowledge/wpf-dark-theme.md for the rule. -->
        <Style x:Key="DarkComboBoxToggleStyle" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="#0F0F22"
                                BorderBrush="#2A2A5E"
                                BorderThickness="1"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="1"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0 Z" Fill="#9D9D9D"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0A84FF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0A84FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1B1B1C"/>
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A2A4D"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0A84FF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#E5E5E5"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Style="{StaticResource DarkComboBoxToggleStyle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="10,0,28,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextElement.Foreground="#E5E5E5"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="#1B1B1C"
                                        BorderBrush="#2A2A5E" BorderThickness="1"
                                        CornerRadius="4"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Install WebDev Plugin"
                       FontSize="16" FontWeight="Bold" Foreground="#E5E5E5"/>
            <TextBlock Text="Pick an official plugin from psmon/AgentZeroLite, or paste a custom Git URL / pick a local .zip below."
                       FontSize="11" Foreground="#9D9D9D" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- ─── Official plugins (runtime-discovered) ───────────────── -->
                <TextBlock Text="OFFICIAL · psmon/AgentZeroLite/Project/Plugins/" Style="{StaticResource SectionHeader}"/>

                <Border Background="#171718" BorderBrush="#2A2A2D" BorderThickness="1" CornerRadius="6" Padding="10">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="cbOfficial" Grid.Column="0"
                                      Style="{StaticResource DarkComboBox}"
                                      FontSize="12"
                                      SelectionChanged="OnOfficialChanged"/>
                            <Button x:Name="btnReloadOfficial" Grid.Column="1" Margin="6,0,0,0"
                                    Content="↻"  ToolTip="Re-fetch the official plugin list from GitHub"
                                    Style="{StaticResource GhostButton}" Padding="10,4" FontSize="13"
                                    Click="OnReloadOfficial"/>
                        </Grid>

                        <Border x:Name="OfficialMetaPanel" Margin="0,8,0,0" Padding="10,8"
                                Background="#0F1014" BorderBrush="#222226" BorderThickness="1" CornerRadius="4"
                                Visibility="Collapsed">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="tbOfficialIcon" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="tbOfficialName" FontSize="13" FontWeight="SemiBold" Foreground="#E5E5E5"/>
                                    <TextBlock x:Name="tbOfficialVersion" FontSize="10" Foreground="#7A7A82" Margin="8,3,0,0"/>
                                </StackPanel>
                                <TextBlock x:Name="tbOfficialId" FontSize="10" Foreground="#5C5C66" FontFamily="Consolas" Margin="0,2,0,0"/>
                                <TextBlock x:Name="tbOfficialDesc" FontSize="11" Foreground="#A8A8B0" TextWrapping="Wrap" Margin="0,6,0,0"/>
                                <TextBlock x:Name="tbOfficialUrl" FontSize="10" Foreground="#5C5C66" FontFamily="Consolas"
                                           TextWrapping="Wrap" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="tbOfficialStatus" FontSize="10" Foreground="#9D9D9D"
                                   TextWrapping="Wrap" Margin="0,6,0,0"
                                   Text="Loading the catalogue from GitHub…"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button x:Name="btnInstallOfficial" Content="Install selected"
                                    Style="{StaticResource PrimaryButton}" IsEnabled="False"
                                    Click="OnInstallOfficial"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ─── Custom (third-party) ────────────────────────────────── -->
                <TextBlock Text="CUSTOM" Style="{StaticResource SectionHeader}" Margin="0,18,0,6"/>

                <Border Background="#171718" BorderBrush="#2A2A2D" BorderThickness="1" CornerRadius="6" Padding="10">
                    <StackPanel>
                        <Button Style="{StaticResource ChoiceButton}" Margin="0,0,0,10" Click="OnPickZip">
                            <StackPanel>
                                <TextBlock Text="📦  From .zip…" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="Local file. Pick a zip with manifest.json at the root or in a single top-level folder."
                                           FontSize="10" Foreground="#9D9D9D" TextWrapping="Wrap" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="Git folder URL" Style="{StaticResource SectionHeader}" Margin="0,4,0,4"/>
                        <TextBox x:Name="tbGitUrl"
                                 Background="#0F0F22" Foreground="#E5E5E5"
                                 CaretBrush="#3DA0FF" BorderBrush="#2A2A5E"
                                 BorderThickness="1" Padding="8,6"
                                 FontFamily="Consolas" FontSize="11"/>
                        <TextBlock Margin="0,6,0,0" FontSize="10" Foreground="#6E6E73" TextWrapping="Wrap">
                            Public GitHub folder URL like https://github.com/owner/repo/tree/main/plugins/my-plugin.
                            The installer fetches manifest.json first, validates it, then downloads every file via the Trees API.
                        </TextBlock>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button x:Name="btnConfirmGit" Content="Install URL"
                                    Style="{StaticResource PrimaryButton}" Click="OnConfirmGit"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Close" Style="{StaticResource GhostButton}" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
