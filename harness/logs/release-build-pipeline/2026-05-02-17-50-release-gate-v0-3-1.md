---
date: 2026-05-02T17:50:00+09:00
engine: release-build-pipeline
trigger: "agent-zero-build skill (릴리즈 배포 수행)"
target_tag: v0.3.1
target_version: 0.3.1
prior_tag: v0.3.0
gate_decision: passed
---

# Release-build-pipeline — v0.3.1 gate

## Step 1 — security-guard (gate)

**Verdict: PASS** — 0 Critical / 0 High / 0 Medium / 0 Low / 2 Info (both pre-existing baseline).

Log: `harness/logs/security-guard/2026-05-02-17-49-release-gate-v0-3-1.md`

Coverage: 7/7 scope items walked. The 9 commits since v0.3.0 are predominantly XAML
styling, harness docs, JS viewer code — only 3 production C# files touched
(`ConPtyTerminalSession`, `MainWindow.xaml.cs` ExePath quoting + DETACH/REDOCK,
`WhisperLocalStt` SEH fallback). No new dependencies, no DLL drops, no migrations,
no actor-creation surface change, no CLI/IPC code change.

Non-blocking follow-ups recorded in the security-guard log (DOMPurify in MD viewer,
cmd metachar escaping in `MainWindow.InitializeTerminal`).

## Step 2 — build-doctor (validation)

**Verdict: PASS** — all 6 validation items clean.

Log: `harness/logs/build-doctor/2026-05-02-17-50-release-gate-v0-3-1.md`

Validations:
- `version.txt` = `0.3.0` (single-line `X.Y.Z`); `BumpVersionAfterBuild` target intact.
- Native DLL pinning resolves on disk for both `conpty.dll` and `Microsoft.Terminal.Control.dll`; `PackageReference` versions match `<Content Include>` hardcoded paths.
- csproj configurations `Debug;Release;AgentCLI` declared and unchanged.
- EF migrations live in `Project/ZeroCommon/Data/Migrations/` (3 files, expected); `Project/AgentZeroWpf/Data/Migrations/` empty.
- `App.OnStartup` `-cli` detection intact (App.xaml.cs:24 → CliHandler.Run + Environment.Exit before WPF Mutex). `coordinated-shutdown.exit-clr = on` at `ActorSystemManager.cs:65`.
- `dotnet build -c Debug --nologo` → 0 errors, 7 warnings (all pre-existing baseline, 0 regressions).

`dotnet test` intentionally not invoked per `harness/knowledge/unit-test-policy.md`.

## Engine decision

Both gates passed. Hand off to `agent-zero-build` skill for version bump, tag, push.

## Notes

- Pre-flight gate was within 24-hour window (security-guard log < 1 min old when build-doctor opened).
- No waivers applied. Both gates were genuine PASS.
- Two Info-level follow-ups from security-guard logged for next maintenance pass; not blocking.
