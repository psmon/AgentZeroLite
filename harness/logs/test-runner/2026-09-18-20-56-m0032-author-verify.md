---
date: 2026-09-18T20:56:00+09:00
agent: test-runner
type: evaluation
mode: execution
trigger: "M0032 수행해 — brief names test execution explicitly (ZeroCommon.Tests + AgentTest) + author-verify for new test classes"
---

# M0032 — test runs

## Why this run

Two reasons, both sanctioned by `harness/knowledge/test-runner/unit-test-policy.md`:
the mission brief asks for test execution outright ("ZeroCommon.Tests·AgentTest 기존
테스트 전부 통과"), and nine new test classes were authored in the task (author-verify).
No LLM-smoke class was added; the LLM-gated tests stayed skipped as usual.

## Scope

| Run | Command | Purpose |
|---|---|---|
| 1 | `dotnet test ZeroCommon.Tests --filter <new classes + FileToolCore + AgentLoopActor + ExternalAgentLoop>` | author-verify of the new classes next to their neighbours |
| 2 | `dotnet test ZeroCommon.Tests` (full) | regression across the headless suite after the catalog change |
| 3 | `dotnet test AgentTest` (full, desktop session) | WPF-side regression: actors, ConPTY, approval parser |
| 4 | `dotnet test ZeroCommon.Tests --filter HeadlessWebFetcher|WebPageExtractor|WearableAgentActor` | re-verify after the security fix (redirect host check) + test race fix |

New classes: `AllowedRootResolverTests`, `AllowedRootFileToolsTests`, `FileOpenPolicyTests`,
`WebPageExtractorTests`, `WebSearchParserTests`, `HeadlessWebFetcherTests`,
`AgentToolCatalogTests`, `Wearable/WearableAgentActorTests` (TestKit), and three methods
added to `Wearable/WearableSettingsTests`.

## Result

| Run | Outcome |
|---|---|
| 1 | 120 total — 119 passed, **1 failed**: `WearableAgentActorTests.Tool_actors_live_under_the_agent` (looked up `/files` before the parent's `PreStart` had created it). Fixed by pinging the agent first (a `Pong` proves PreStart ran) and awaiting `ResolveOne` instead of `.Result`. |
| 2 | **787 passed, 0 failed, 24 skipped** (50 s) |
| 3 | **146 passed, 0 failed, 7 skipped** (1 s reported; desktop session present) |
| 4 | **36 passed, 0 failed** |

Skips are the standing LLM / model-file gates (`Xunit.SkippableFact`), unchanged by this
mission.

## 평가

| Axis | Result |
|---|---|
| Policy compliance | Pass — runs justified by brief + author-verify; no unrequested LLM smoke |
| Signal quality | The one failure was a real ordering assumption in the test, not flake; the fix removes the race rather than adding a sleep |
| Coverage of the change | Every new pure class has a test; the actor subtree is covered end-to-end through `WearableToolbelt` → actors → JSON |

## 다음 단계 제안

- None for the runner. Coverage gaps (WebView2-backed read, redirect-follow policy) are
  listed in the code-coach log as future work.
