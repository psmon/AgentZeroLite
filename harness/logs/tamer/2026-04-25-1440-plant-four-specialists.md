---
date: 2026-04-25T14:40:00+09:00
agent: tamer
type: creation
mode: suggestion-tip
trigger: "3명 다 심어줘 (+ 코드코치)"
---

# Plant Four Specialists — Initial Cohort

## Execution Summary

User accepted the onboarding proposal and added a fourth agent (`code-coach`) with
a specific dual-mode brief (manual + auto pre-commit review + research consult).

Created the following under `harness/`:

- `agents/security-guard.md`
- `agents/build-doctor.md`
- `agents/test-sentinel.md`
- `agents/code-coach.md`
- `engine/release-build-pipeline.md` — security-guard → gate → build-doctor
- `engine/pre-commit-review.md` — code-coach auto-invoke before `git commit`
- `knowledge/agentzerolite-architecture.md`
- `knowledge/security-surface.md`
- `docs/v1.1.0.md`

Updated:
- `harness.config.json` — version 1.0.0 → 1.1.0; agents [tamer] → [tamer +4];
  engine [] → [release-build-pipeline, pre-commit-review].

Memory pinning (cross-session durability):
- `memory/project_release_security_gate.md` — agent-zero-build must call
  release-build-pipeline before tagging.
- `memory/project_pre_commit_code_coach.md` — auto-invoke code-coach before commit
  when staged diff has dev code.

## Result

| Layer | Before | After |
|-------|--------|-------|
| knowledge/ | 0 | 2 |
| agents/ | 1 | 5 |
| engine/ | 0 | 2 |
| docs/ | 1 (README) | 2 (+ v1.1.0) |

Trigger surface:

- `security-guard`: 5 triggers (English + Korean variants)
- `build-doctor`: 5 triggers, gated invocation from `release-build-pipeline`
- `test-sentinel`: 4 triggers
- `code-coach`: 11 triggers (3 modes — manual / auto-precommit / research)
- `release-build-pipeline`: 5 triggers + auto-invoke from `agent-zero-build` skill
- `pre-commit-review`: 2 explicit triggers + auto-invoke condition documented

All triggers stay inside `harness/agents/*.md` and `harness/engine/*.md` frontmatter
— **no pollution of the standard `harness-kakashi-creator/SKILL.md`** (per
`references/skill-separation.md`: project-specific terminology + project file paths +
project-only build pipeline → project-side only).

## Evaluation (3-axis)

### Axis 1 — Workflow Improvement: **A**

Two workflows added where there were zero. The release-build-pipeline introduces a
hard gate on security review before any tagged release — this directly mechanizes
the README's Security Notice promise to end-users (was vibes; now code). The
pre-commit-review folds code review into normal commit flow without forcing it on
doc-only commits. Both close real loops.

### Axis 2 — Skill Utilization: **4**

Engine `release-build-pipeline` explicitly hands off to the existing
`agent-zero-build` skill instead of duplicating its logic — proper delegation.
Engine `pre-commit-review` explicitly does NOT install a hook (would be too coarse)
and instead pins behavior via memory + frontmatter convention — composes with the
`update-config` skill (used earlier this session for `git push` permission) by
**not** competing with it. Code-coach Mode 3 (research) is positioned to optionally
write knowledge notes, leaving a path to involve the `claude-api` / `browser-test`
skills for live API or browser research without overlapping their domains. Minor
gap: no cross-link yet to the `claude-code-guide` agent for harness questions —
left for the next pass.

### Axis 3 — Harness Maturity: **L3 (성장)**

Threshold per `references/evaluation.md`: 2~3 agents + 1+ workflow → L3. Now have
5 agents (tamer + 4 specialists) and 2 workflows. Knowledge layer seeded with the
two highest-leverage docs (architecture map + security surface). Logging is live
from this entry forward.

To reach L4 (안정): need accumulated logs across multiple agent runs + repeat
feedback loop activations + at least one knowledge doc generated by code-coach
Mode 3 from a real research request.

## Next Steps

1. Run `security-guard` once on the current `HEAD` to seed a baseline log.
2. Run `test-sentinel` to baseline current coverage and flag the boundary status.
3. Wait for the next `git commit` of dev code — `pre-commit-review` should fire
   automatically and produce the first `code-coach` log.
4. On the next release attempt — `release-build-pipeline` should kick in via the
   `agent-zero-build` skill; verify the gate works end-to-end.
