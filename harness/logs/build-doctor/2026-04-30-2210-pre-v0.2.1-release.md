---
date: 2026-04-30 22:10
agent: build-doctor
trigger: release-build-pipeline (pre-v0.2.1 gate)
upstream-log: harness/logs/security-guard/2026-04-30-2203-pre-v0.2.1-release.md
gate-status: PASS
---

# Build Doctor — pre-v0.2.1 release gate

## Security gate
- security-guard PASS at 22:03 (8-commit delta, 0 Critical / 0 High / 0 Medium / 0 Low / 1 Info).
- Within the required 24h window. Hard gate satisfied.

## Scope checks

### 1. Version pipeline
- `Project/AgentZeroWpf/version.txt` = `0.2.0` (current). Target `0.2.1` (patch — frontend-only delta).
- `BumpVersionAfterBuild` post-build target untouched since last release.
- `agent-zero-build` skill present and is the orchestrator for tag+push.

### 2. Native DLL pinning
Both `<Content Include=…>` paths in `Project/AgentZeroWpf/AgentZeroWpf.csproj` resolved on disk:

| Pin | Disk |
|-----|------|
| `ci.microsoft.windows.console.conpty/1.22.250314001/runtimes/win10-x64/native/conpty.dll` | ✓ exists |
| `ci.microsoft.terminal.wpf/1.22.250204002/runtimes/win-x64/native/Microsoft.Terminal.Control.dll` | ✓ exists |

No DLL/package drift in the 8-commit delta (`git diff cbee04e^..2ae8b75 -- '*.csproj'` shows no `<PackageReference>` / `<Content Include>` changes).

### 3. Build configurations
`<Configurations>Debug;Release;AgentCLI</Configurations>` preserved.

### 4. Output assembly
`Project/AgentZeroWpf/bin/Debug/net10.0-windows/AgentZeroLite.exe` produced (Debug build clean, 0 errors). `App.OnStartup` `-cli` detection unchanged in this delta.

### 5. EF migrations location
- `Project/ZeroCommon/Data/Migrations/`: `20260421061855_InitialCreate.{cs,Designer.cs}` + `AppDbContextModelSnapshot.cs` — correct.
- `Project/AgentZeroWpf/Data/Migrations/`: empty — correct.
- No new migrations in the 8-commit delta.

### 6. Release workflow
`.github/workflows/release.yml` present, untouched in this delta (`git diff cbee04e^..2ae8b75 -- '.github/'` empty). Tag-push will fire it as designed.

### 7. Crash dump forensics correlation
None present in working tree (security-guard scope item #7 confirmed empty).

## Build smoke
```
dotnet build Project/AgentZeroWpf/AgentZeroWpf.csproj -c Debug
→ 오류 0개 (8.64s)
```

## Test execution
**Skipped intentionally.** Per user call: 8-commit delta is purely UI/diagnostic (AgentZeroWpf project + `ITerminalSession` interface mock surface). Headless test suite (ZeroCommon.Tests) covers ApprovalParser, AgentToolLoop, etc. — none exercise the new `WriteAndEnter` / `HealthState` paths. Build success already validates the interface signatures + mock compatibility. Per `harness/knowledge/dotnet-test-execution.md` canon R5 (skip optional WPF-dependent suite when not exercising the changed surface).

`tasklist | grep -iE "testhost|vstest"` → empty (canon R3+R6).

## Rubric

| Axis | Score |
|------|-------|
| Build correctness | A — Debug build 0 errors |
| Native DLL pinning | Pass — both paths resolve |
| Version pipeline integrity | A — version.txt + post-build + agent-zero-build skill aligned |
| Security-gate compliance | Pass — security-guard ran 22:03, 0 Critical/High |
| Test-execution hygiene | Pass — no orphan testhost, single-foreground discipline (no test run by design here) |

## Decision

**PASS** — proceed with version bump → tag → push → GitHub Actions handoff.
