---
gate: release-build-pipeline (pre-tag for v0.3.2)
head: 87cbe34
prior_release: v0.3.1
verdict: pass
build_errors: 0
build_warnings: 7 (all pre-existing, unrelated)
security_gate: pass (logs/security-guard/2026-05-03-01-22-release-gate-v0-3-2.md)
---

# Pre-release build gate — v0.3.2

## Scope coverage

### 1. Version pipeline
- `Project/AgentZeroWpf/version.txt` = `0.3.1` (will bump to `0.3.2`).
- `BumpVersionAfterBuild` target wired to `Configuration == Release`.
- `<VersionFile>` points at the canonical path. Workflow's `steps.ver`
  reads the same file (line 43 of release.yml).

### 2. Native DLL pinning
- `conpty.dll` pin: `ci.microsoft.windows.console.conpty/1.22.250314001`
  → resolved on disk ✓
- `Microsoft.Terminal.Control.dll` pin:
  `ci.microsoft.terminal.wpf/1.22.250204002` → resolved on disk ✓
- New `<Content Include="Wasm\**\*">` (added in v0.3.2 cycle) — local
  HTML/CSS/JS, not a native pin concern.

### 3. Build configurations
- `<Configurations>Debug;Release;AgentCLI</Configurations>` intact
  (csproj line 15).

### 4. Output assembly + runtime mode split
- `App.OnStartup` (`App.xaml.cs:24`):
  `e.Args.Contains("-cli", StringComparer.OrdinalIgnoreCase)` ✓.
- `AssemblyName = AgentZeroLite` (single exe handles both modes).

### 5. EF migrations location
- `Project/ZeroCommon/Data/Migrations/` is the only one populated:
  `20260421061855_InitialCreate.cs` + designer + snapshot.
- `Project/AgentZeroWpf/Data/Migrations/` does not exist on disk ✓.

### 6. Release workflow
- `.github/workflows/release.yml` consumes `version.txt` (line 43),
  publishes `-r win-x64`, zips `AgentZeroLite-${tag}-win-x64.zip`,
  runs `iscc /DAppVersion=...` against `installer/AgentZeroLite.iss`.
- Artifact path matches `agent-zero-build` skill's expected output.

### 7. Build-environment correlation
- No tracked crash dumps surfaced in security-guard step. N/A.

## Build verification
```
dotnet build Project/AgentZeroWpf/AgentZeroWpf.csproj -c Debug -v minimal
```
- Result: 0 errors, 7 warnings (all pre-existing: CS0169/0649/0414 on
  unused fields + NETSDK1206 RID notice). None introduced by this cycle.
- Elapsed: 4.89s.

## Verdict
PASS — release-build-pipeline gate cleared. Hand off to `agent-zero-build`
skill for version bump, tag, and GitHub Actions trigger.
