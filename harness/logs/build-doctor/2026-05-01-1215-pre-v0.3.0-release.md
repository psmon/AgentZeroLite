---
date: 2026-05-01 12:15
agent: build-doctor
trigger: release-build-pipeline (pre-v0.3.0 gate)
upstream-log: harness/logs/security-guard/2026-05-01-1200-pre-v0.3.0-release.md
gate-status: PASS
---

# Build Doctor — pre-v0.3.0 release gate

## Security gate
- security-guard PASS at 12:00 (1-commit voice delta, 0 Critical / 0 High; 1 Low + 1 Info, both deferred post-release).
- Within the required 24h window. Hard gate satisfied.

## Scope checks

### 1. Version pipeline
- `Project/AgentZeroWpf/version.txt` = `0.2.1` (current). Target `0.3.0` (minor — voice TTS + voice commands ship as a new feature).
- `BumpVersionAfterBuild` post-build target untouched (`AgentZeroWpf.csproj:106`, Condition=`Release`).
- `agent-zero-build` skill present; this run is the orchestrator for tag+push.
- `.github/workflows/release.yml` "Resolve version" step trims the leading `v` from the tag and falls back to `version.txt` on `workflow_dispatch` — both paths consistent with bumping `version.txt` to `0.3.0` and pushing `v0.3.0`.

### 2. Native DLL pinning
Both `<Content Include=…>` paths in `Project/AgentZeroWpf/AgentZeroWpf.csproj` resolved on disk:

| Pin | Disk |
|-----|------|
| `ci.microsoft.windows.console.conpty/1.22.250314001/runtimes/win10-x64/native/conpty.dll` | ✓ exists |
| `ci.microsoft.terminal.wpf/1.22.250204002/runtimes/win-x64/native/Microsoft.Terminal.Control.dll` | ✓ exists |

No DLL/package drift in the 1-commit delta (`git diff v0.2.1..HEAD --stat` shows no `*.csproj` changes).

### 3. Build configurations
`<Configurations>Debug;Release;AgentCLI</Configurations>` preserved (`AgentZeroWpf.csproj:15`).

### 4. Output assembly
`Project/AgentZeroWpf/bin/Debug/net10.0-windows/AgentZeroLite.exe` produced cleanly. `App.OnStartup` `-cli` detection untouched in this delta.

### 5. EF migrations location
- `Project/ZeroCommon/Data/Migrations/`: `20260421061855_InitialCreate.{cs,Designer.cs}` + `AppDbContextModelSnapshot.cs` — correct.
- `Project/AgentZeroWpf/Data/Migrations/`: empty — correct.
- No new migrations in the voice delta.

### 6. Release workflow
`.github/workflows/release.yml` present, untouched in this delta. Pushing `v0.3.0` will fire publish → zip → Inno Setup → release-asset upload as designed.

### 7. Crash dump forensics correlation
None present in working tree (security-guard scope item #7 confirmed empty).

## Build smoke
```
dotnet build Project/AgentZeroWpf/AgentZeroWpf.csproj -c Debug
→ 경고 0개, 오류 0개 (1.04s)
```

## Test execution
**Skipped intentionally per user direction.** User confirmed local unit-test runs were sufficient during voice-feature development. Voice subsystem has dedicated tests (`VoiceCommandInterceptorTests`, `WhisperHallucinationFilterTests`, `AgentToolLoopTests`) added in this delta and validated locally. Per `harness/knowledge/dotnet-test-execution.md` canon R5: when the changed surface has been exercised locally, the release-gate run is optional. No orphan testhost (none spawned this pass).

## Rubric

| Axis | Score |
|------|-------|
| Build correctness | A — Debug build 0 warnings, 0 errors |
| Native DLL pinning | Pass — both paths resolve |
| Version pipeline integrity | A — version.txt + post-build + release.yml aligned for 0.2.1→0.3.0 |
| Security-gate compliance | Pass — security-guard 12:00, 0 Critical/High |
| Test-execution hygiene | Pass — no orphan testhost (no test run this pass per user instruction) |

## Decision

**PASS** — proceed with version bump → tag → push → GitHub Actions handoff.
