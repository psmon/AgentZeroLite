---
date: 2026-05-02
agent: build-doctor
mode: gated (release-build-pipeline)
trigger: agent-zero-build skill (post security-guard PASS)
target_tag: v0.3.1
verdict: pass
---

# Build Doctor — Release Gate (v0.3.1)

## Pre-flight gate

`security-guard` produced a clean log for the same target tag earlier today:

- Log: `harness/logs/security-guard/2026-05-02-17-49-release-gate-v0-3-1.md`
- Verdict: **pass** (0 Critical, 0 High)
- Age: < 1 minute. Well within the 24-hour gate window.

Gate satisfied. Proceeding with build validation.

## 1. Version pipeline

**File**: `Project/AgentZeroWpf/version.txt:1` → `0.3.0` (single line, no BOM, trailing newline).

**Format expectation**: `X.Y.Z` SemVer string on a single line. The csproj reads it via
`File::ReadAllText(...).Trim()` so trailing newline is fine, but no second line is allowed.

**Bump target**: `Project/AgentZeroWpf/AgentZeroWpf.csproj:106-125`
`<Target Name="BumpVersionAfterBuild" AfterTargets="Build" Condition="'$(Configuration)' == 'Release'">`

The target reads `version.txt`, splits on `.`, increments patch, and rolls patch→minor→major
on `>=10`. Logic is unchanged from the v0.3.0 release. Since the `agent-zero-build` skill
writes `version.txt` itself before tagging, the post-build target only matters for the
*next* release cycle. For v0.3.1 the skill will overwrite `0.3.0` → `0.3.1` and the post-
build target (if Release is invoked) will then bump it again to `0.3.2`. That sequence is
the documented behavior.

**Assembly version wiring** (csproj:96-103): `version.txt` feeds `Version`,
`AssemblyVersion`, `FileVersion`, `InformationalVersion`. `version.txt` is also copied to
output (csproj:89-93) so CLI `-cli status` reports match the file the installer used.

**Verdict**: A — pipeline integrity intact.

## 2. Native DLL pinning

Two `<Content Include=...>` entries (`Project/AgentZeroWpf/AgentZeroWpf.csproj:128-137`):

| Path in csproj | On disk | Status |
|---|---|---|
| `$(NuGetPackageRoot)/ci.microsoft.windows.console.conpty/1.22.250314001/runtimes/win10-x64/native/conpty.dll` | present at `~/.nuget/packages/ci.microsoft.windows.console.conpty/1.22.250314001/runtimes/win10-x64/native/conpty.dll` | **OK** |
| `$(NuGetPackageRoot)/ci.microsoft.terminal.wpf/1.22.250204002/runtimes/win-x64/native/Microsoft.Terminal.Control.dll` | present at `~/.nuget/packages/ci.microsoft.terminal.wpf/1.22.250204002/runtimes/win-x64/native/Microsoft.Terminal.Control.dll` | **OK** |

Both version segments (`1.22.250314001`, `1.22.250204002`) are the only versions installed
under `~/.nuget/packages/`. PackageReference for `CI.Microsoft.Terminal.Wpf` in csproj:23
specifies `1.22.250204002` — exact match.

**Output verification**: post-build, both DLLs land in
`Project/AgentZeroWpf/bin/Debug/net10.0-windows/`:

- `conpty.dll` (108,576 bytes)
- `Microsoft.Terminal.Control.dll` (1,989,144 bytes)
- `AgentZeroLite.exe` (243,200 bytes) — assembly name correct

**Verdict**: Pass — all `<Content Include>` paths resolve and copy.

## 3. csproj configurations

`Project/AgentZeroWpf/AgentZeroWpf.csproj:15` →
`<Configurations>Debug;Release;AgentCLI</Configurations>`

All three configurations declared. The runtime CLI/GUI split lives in
`Project/AgentZeroWpf/App.xaml.cs:24` (`-cli` arg detection), not in the configuration.
The `AgentCLI` config is preserved as required.

**Verdict**: Pass.

## 4. EF migrations location

| Folder | Contents | Expected |
|---|---|---|
| `Project/ZeroCommon/Data/Migrations/` | `20260421061855_InitialCreate.cs`, `.Designer.cs`, `AppDbContextModelSnapshot.cs` | yes — canonical |
| `Project/AgentZeroWpf/Data/Migrations/` | empty | yes — must stay empty |

No accidental scaffold into the WPF folder. `ZeroCommon/Data/Migrations/` is the only
migration source, matching CLAUDE.md's persistence contract.

**Verdict**: Pass.

## 5. App.OnStartup `-cli` detection

`Project/AgentZeroWpf/App.xaml.cs:19-31`:

```csharp
protected override void OnStartup(StartupEventArgs e)
{
    base.OnStartup(e);
    // CLI mode: run console handler and exit without showing WPF window
    if (e.Args.Contains("-cli", StringComparer.OrdinalIgnoreCase))
    {
        int exitCode = CliHandler.Run(e.Args);
        Console.Out.Flush();
        Console.Error.Flush();
        NativeMethods.FreeConsole();
        Environment.Exit(exitCode);
    }
    // GUI 모드: 단일 인스턴스 가드  (Mutex created AFTER the -cli short-circuit)
    ...
}
```

Confirmed:

- `-cli` check happens **before** the single-instance Mutex (line 24, vs. Mutex at line 34)
- `CliHandler.Run` is invoked, then `Environment.Exit(exitCode)` — WPF message loop never starts
- `coordinated-shutdown.exit-clr = on` is set in
  `Project/AgentZeroWpf/Actors/ActorSystemManager.cs:65` (HOCON config block lines 63-66
  also set `terminate-actor-system = on`, `run-by-clr-shutdown-hook = on`)

**Verdict**: Pass — single-exe dual-mode contract intact.

## 6. Clean compile (Debug)

```
dotnet build Project/AgentZeroWpf/AgentZeroWpf.csproj -c Debug --nologo
```

- **Errors**: 0
- **Warnings**: 7 (vs. baseline 7 at v0.3.0 — no new warnings)
- **Elapsed**: 9.31 s
- **Output**: `D:\Code\AI\AgentZeroLite\Project\AgentZeroWpf\bin\Debug\net10.0-windows\AgentZeroLite.dll` and `AgentZeroLite.exe`

Warning breakdown (all pre-existing, not introduced by this release window):

1. `NETSDK1057` — .NET preview-version notice (informational, expected for net10.0)
2. `CS0649` SettingsPanel.Llm.cs:35 `_hasLoadedOnceInProcess` field never assigned (×2 — wpftmp + main)
3. `CS0169` MainWindow.xaml.cs:927 `_scrapWindow` field unused (×2)
4. `CS0414` AgentBotWindow.xaml.cs:104 `_mdPickerSelectedIndex` assigned but unused (×2)
5. `NETSDK1206` versioned RID notice for `CI.Microsoft.Windows.Console.ConPTY`
   (`win10-arm64`, `win10-x64`, `win10-x86`) — known package metadata legacy, harmless

None of these are blockers. None changed between v0.3.0 and HEAD.

**Verdict**: A — clean compile, no error, no warning regression.

## 7. Build-environment crash correlation

No `*.stackdump` / `*.dmp` artifacts surfaced in the matching `security-guard` log
(`2026-05-02-17-49-release-gate-v0-3-1.md`). No build-environment hardening required.

## Decision

**PASS**. All six validation items clear. Release v0.3.1 may proceed to:

1. version.txt write `0.3.0` → `0.3.1` (skill-owned)
2. annotated tag push
3. GitHub Actions release workflow handoff

## Self-evaluation

| Axis | Measure | Score |
|---|---|---|
| Build correctness | Debug build clean, 0 errors, 7 warnings (no regression) | A |
| Native DLL pinning | Both `<Content Include>` paths resolve; DLLs in output dir | Pass |
| Version pipeline integrity | version.txt format OK, BumpVersionAfterBuild target intact, AssemblyVersion wiring correct | A |
| Security-gate compliance | security-guard pass log present and current (< 1 min old) | Pass |
| Scope discipline | did NOT invoke `dotnet test` (test-runner owns that) | Pass |

No follow-ups. Hand off to `agent-zero-build` skill for tag/push.
