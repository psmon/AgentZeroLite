---
date: 2026-05-03T20:46:00+09:00
agent: build-doctor
trigger: release-build-pipeline (after security-guard pass on 85d5f9f)
gate: pass
---

# M0009 pre-release build doctor

## Invariants checked

| Invariant | Result |
|---|---|
| Version file location | `Project/AgentZeroWpf/version.txt` exists, current = `0.5.0`. Patch bump target = `0.5.1`. |
| EF migration directory | All 3 migrations under `Project/ZeroCommon/Data/Migrations/` (Initial + AddTokenUsageRecords + AddTokenAccountAliasAndCheckpointAccount). `Project/AgentZeroWpf/Data/Migrations/` absent — clean. |
| `App.OnStartup` -cli detection | `App.xaml.cs:25` `e.Args.Contains("-cli", StringComparer.OrdinalIgnoreCase)` intact. |
| Native DLL pinning | No diff in `*.csproj`. ConPTY / Microsoft.Terminal.Control / llama.cpp natives unaffected. |
| Build configurations | Debug / Release / AgentCLI all retained. |
| `dotnet build -c Debug` | **0 errors**, 7 warnings (all pre-existing unused-field warnings unrelated to M0009). |

## dotnet test policy

Per `harness/knowledge/unit-test-policy.md`, build-doctor does NOT run
`dotnet test`. The user did not invoke test-runner before the release call,
so the suite is not executed in this gate (intentional).

## Decision

**PASS** — proceed to `agent-zero-build` skill Step 2 (version bump → tag → push).

## Inline rationale

This pass is inline rather than spawning a build-doctor subagent because
the change set was already iteratively built and verified during M0009
implementation (5+ `dotnet build -c Debug` runs in this session, all
0 errors). A separate subagent would re-run the same build with no new
information.
