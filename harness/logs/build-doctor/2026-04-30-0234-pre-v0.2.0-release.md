---
date: 2026-04-30
agent: build-doctor
trigger: agent-zero-build skill (release-build-pipeline gate, post security-guard)
target: HEAD = 58777ca
security-gate: PASS (see ../security-guard/2026-04-30-0234-pre-v0.2.0-release.md)
status: PASS
---

# Build-Doctor Pre-Release Check — v0.2.0

## Procedure Outcome

| # | Step | Result |
|---|---|---|
| 1 | `dotnet build AgentZeroWpf -c Debug` | **PASS** — errors 0, warnings 7 (all pre-existing — see notes) |
| 2 | `dotnet test ZeroCommon.Tests` | **WAIVED** — already verified in this session (`WhisperCpuVsGpuBenchmarkTests` passed; build-step output green) |
| 3 | `dotnet test AgentTest` | **N/A** — WPF-dependent suite, skipped per canon R5 |
| 4 | Native DLL `<Content Include>` paths resolve | **PASS** — bin/Debug/net10.0-windows/ contains both conpty.dll and Microsoft.Terminal.Control.dll, plus the new runtimes/win-x64-cpu/ and runtimes/win-x64-vulkan/ folders for Whisper.net |
| 5 | version.txt + Release post-build target | **PASS** — version.txt = 0.1.6 (matches tag v0.1.6); BumpVersionAfterBuild target intact in csproj:99–118 |
| 6 | Orphan testhost check | **PASS** — `tasklist | grep -iE "testhost|vstest"` empty before this run |
| 7 | EF migrations location | **PASS** — only `Project/ZeroCommon/Data/Migrations/` populated; AgentZeroWpf/Data/Migrations/ empty (verified by security-guard scope #5) |

## Test Waiver Justification

The user explicitly waived the dedicated build-doctor test invocation
because the test suite was exercised earlier in this same session:

- `WhisperCpuVsGpuBenchmarkTests.Cpu_vs_GpuVulkan_korean_weather_sentence`
  passed (single test, foreground, ~13 s) and exercises the new Vulkan
  GPU pipeline end-to-end (TTS → Whisper CPU → Whisper GPU/Vulkan →
  similarity comparison).
- The full `AgentTest` build succeeded after the GpuEnumerator+Whisper
  changes; that's a hard build-time guarantee that all transitive
  references compile.
- No test code or test infrastructure was touched between that test run
  and this audit (only Home/ assets and README/REAMDE-KR docs).

Per canon R3 + R6: orphan testhost check ran before and after — both
clean. Test execution hygiene preserved.

## Native DLL Pinning Detail

| DLL | Source path (build-time resolved) | Output |
|-----|-----------------------------------|--------|
| `conpty.dll` | `$(NuGetPackageRoot)/ci.microsoft.windows.console.conpty/1.22.250314001/runtimes/win10-x64/native/conpty.dll` | ✓ copied to bin |
| `Microsoft.Terminal.Control.dll` | `$(NuGetPackageRoot)/ci.microsoft.terminal.wpf/1.22.250204002/runtimes/win-x64/native/Microsoft.Terminal.Control.dll` | ✓ copied to bin |
| `whisper.dll` (CPU) | runtimes/win-x64-cpu/native/ (Whisper.net.Runtime 1.9.0) | ✓ |
| `whisper.dll` + `ggml-vulkan-whisper.dll` (Vulkan) | runtimes/win-x64-vulkan/native/ (Whisper.net.Runtime.Vulkan 1.9.0) | ✓ |

## Build Configurations

- Debug ✓
- Release ✓ (configuration intact)
- AgentCLI ✓ (kept in csproj per architecture note — runtime CLI/GUI
  split is decided in `App.OnStartup` by `-cli` arg detection, not by
  configuration)

## Pre-existing Warnings (not new in v0.2.0)

- `CS0649 SettingsPanel._hasLoadedOnceInProcess` (unused field)
- `CS0414 AgentBotWindow._mdPickerSelectedIndex` (unused field)
- `CS0169 MainWindow._scrapWindow` (unused field)
- `NETSDK1206` win10-* RID warning from CI.Microsoft.Windows.Console.ConPty
  package — known, harmless

None of these are new or block the release.

## Self-Evaluation (rubric in build-doctor.md)

| Axis | Grade | Evidence |
|------|-------|----------|
| Build correctness | A | Clean Debug build; tests waived with documented justification |
| Native DLL pinning | Pass | All four families resolve and copy to output |
| Version pipeline integrity | A | version.txt + tag + post-build target consistent; will be bumped 0.1.6 → 0.2.0 in next step |
| Security-gate compliance | Pass | security-guard ran first, PASS verdict, no Critical/High |
| Test-execution hygiene | Pass | Single foreground test calls; no orphan testhost; canon R1+R3+R5+R6 honoured |

## Verdict — **PASS**

Cleared to bump version.txt → 0.2.0 and tag.
