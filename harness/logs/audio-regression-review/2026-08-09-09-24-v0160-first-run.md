---
date: 2026-08-09T09:24:00+09:00
engine: audio-regression-review
type: review
trigger: "오디오 회귀 점검 수행"
scope_range: v0.15.0..v0.16.0
verdict: advisory
lanes_fired: [voice-curator]
lanes_noop: [music-curator]
linked_logs:
  - harness/logs/voice-curator/2026-08-09-09-24-v0160-audio-regression-review.md
---

# audio-regression-review — 첫 실전 실행 (v0.16.0)

## 변경 감지 라우팅 (Step 1–2)

범위 `v0.15.0..v0.16.0` (방금 출하한 릴리스, 큐레이터 검수 없이 통과한 오디오
change class). watched-path 매칭:

| 레인 | 결과 | 근거 |
|------|------|------|
| **music-curator** | **no-op** | agent-band 변경은 스프라이트 에셋(play.json/png 26종) + `ZeroCommon/Mp3/PathAvailability.cs`. `ZeroCommon/Music/**`·`Services/Music/**`·`LoopbackCaptureService` 미해당 → 음악 도메인 표면 아님 |
| **voice-curator** | **fired** | `Voice/Diarization/SherpaSpeakerDiarizer.cs` + `Services/Browser/WebDevHost.cs`(+`.Mp3.cs`) 변경 감지 |

## 판정 (Step 5)

**verdict = advisory.**

voice 레인 리뷰(linked)에서 코드 정확성 축(Model-pair / Cross-source /
UI-responsiveness / Cross-model-extensibility) **전원 Pass** — v0.16.0의
diarization 청크 스코프 전환 + 동시 추론 직렬화는 건전하게 출하됨.

유일한 Fail = **Knowledge capture** — 청크 스코프 메모리 모델 + `_noteInferGate`
직렬화 계약이 로그 4건에만 존재하고 `harness/knowledge/voice-curator/`에 미반영.
이는 correctness Fail이 아니라 문서 완결성 축이므로 엔진 verdict 규칙상
recommend-block이 아닌 **advisory**로 분류(엔진 recommend-block은 broken
format/wrong pair/non-16kHz 같은 런타임 correctness Fail에 한정).

릴리스는 이미 출하됐으므로 tag-hold 개념의 recommend-block은 소급 무의미.

## 엔진 rubric 자기평가

| 축 | 판정 | 근거 |
|----|------|------|
| Routing accuracy | **Pass** | watched-path 매칭이 정확 — voice 3파일 발동, music은 에셋/mp3만이라 정확히 no-op. false fire/missed lane 없음 |
| No-op discipline | **Pass** | music 레인은 큐레이터 미호출로 값싸게 종료 |
| Verdict calibration | **A** | correctness 축 Pass ↔ knowledge-capture Fail을 conflate하지 않고 advisory로 정확히 분류 |
| Engine log shape | **Pass** | 집계 전용 — voice 로그 링크, 소견 중복 서술 안 함(Rule 6) |

## 후속 (엔진이 operator에게 넘기는 결정)

- **[필수]** voice-curator 지식 신설 (청크 스코프 메모리 모델 + 직렬화 계약).
  linked voice 로그 "다음 단계" 참조. → "새 지식 추가해"로 진행 가능.
- **[열린 검증]** 실기기 5분+ 세션 메모리 안정성 (`[Diar] chunk-scoped` 로그).

## 메타

엔진 codification(v1.10.0) 이후 첫 실행. `harness/engine/audio-regression-review.md`
Status 섹션이 예고한 대로, v0.16.0이 바로 이 엔진이 라우팅했어야 할 change class
였음이 실측으로 확인됨(voice fired / music no-op).
