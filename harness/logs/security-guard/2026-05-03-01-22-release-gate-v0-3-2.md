---
gate: release-build-pipeline (pre-tag for v0.3.2)
head: 87cbe34 fix(webdev): keep WebView2 alive across Settings tab switches
prior_release: v0.3.1
verdict: pass
critical: 0
high: 0
medium: 0
low: 0
---

# Pre-release security gate — v0.3.2

## Scope coverage (all 7)

### 1. Injection surface
- New IPC surface added since v0.3.1: `WebDevBridge.cs` (chrome.webview
  postMessage). Dispatch is a hard switch over `{version, voice.providers,
  tts.speak, tts.stop}` with `default → throw InvalidOperationException`. No
  reflection, no eval, no shell exec. The `tts.speak.text` argument flows
  only to `WindowsTts` / `OpenAiTts` synthesis APIs. Not a command surface.
- `WebDevHost.SpeakAsync` does not invoke `Process.Start`, does not write to
  `ITerminalSession`, does not touch the shell. Safe.

### 2. Actor name sanitization
- No new actor paths introduced since v0.3.1. WebDev sits outside the
  Akka topology entirely.

### 3. CLI ↔ GUI IPC
- `WM_COPYDATA` / MMF surface unchanged since v0.3.1.

### 4. Native binary trust
- New native dependency since v0.3.1: `Microsoft.Web.WebView2 1.0.3124.44`
  (official Microsoft package, signed; WebView2 Runtime is OS-distributed
  via Edge Update). Acceptable.
- Existing `conpty.dll` / `Microsoft.Terminal.Control.dll` `<Content Include>`
  pinning unchanged.
- New `<Content Include="Wasm\**\*">` ships local HTML/CSS/JS as build
  output. WebView2 mounts `Wasm/` at `https://zero.local/` via
  `SetVirtualHostNameToFolderMapping`. The web app loads only local assets;
  no remote navigation is initiated by code (`Navigate(https://zero.local/...)`
  only). Safe.

### 5. Persistence
- EF migrations only in `Project/ZeroCommon/Data/Migrations/` (verified).
  `Project/AgentZeroWpf/Data/Migrations/` does not exist on disk.
- Seeded `CliDefinition` rows still gated by `IsBuiltIn=true` in
  `SettingsPanel.xaml.cs`.

### 6. Dependency drift since v0.3.1
- `Microsoft.Web.WebView2 1.0.3124.44` — added (signed Microsoft package).
- No other PackageReference additions.

### 7. Crash dump forensics
- `git ls-files | grep -E '\.(stackdump|dmp|crash)$|^core$|^crash-'` →
  empty. No tracked dumps.
- No `*.stackdump` / `*.dmp` in working tree.

## Findings
None.

## Verdict
PASS — no Critical / High findings. Build-doctor may proceed.
