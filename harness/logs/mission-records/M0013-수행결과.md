---
mission: M0013
date: 2026-05-05
agent: code-coach
type: refactor
mode: log-eval
trigger: "지금 Actor 모델이 LLM을 Agent화 하는부분 네이밍이랑 컨셉 설명 해줄래? Agent가까운 느낌으로 이름변경 리팩토링을 하려고함"
status: completed
---

# Mission M0013 — Reactor → AgentLoop 리네임 (Agent vocabulary 정렬)

## 실행 요약

`harness/missions/M0013-agent-loop-rename.md` 의 **Option B** 매핑을 그대로
실행. 의미 변경 0, 어휘만 정리. Akka path `/user/stage/bot/reactor` →
`/user/stage/bot/loop`. CLAUDE.md actor topology 블록 갱신 + 신규 지식 노트
`harness/knowledge/_shared/agent-architecture.md` 발행.

## 결과 — 변경 surface

20 파일 (rename + edit):

| 영역 | 신규 파일 (rename 포함) |
|---|---|
| Actors | `AgentLoopActor.cs` (← `AgentReactorActor.cs`), `AgentBotActor.cs`, `Messages.cs` |
| Llm/Tools | `IAgentLoop.cs`, `LocalAgentLoop.cs`, `ExternalAgentLoop.cs`, `IAgentToolbelt.cs`, `AgentLoopGuards.cs`, `ChatTemplates.cs`, `T0Probe.cs` |
| WPF host | `AgentBotWindow.xaml.cs`, `WorkspaceTerminalToolHost.cs`, `LlmRuntimeSettings.cs` |
| Tests | `AgentLoopActorTests.cs`, `AgentLoopTests.cs`, `ExternalAgentLoopTests.cs`, `AgentLoopGuardsTests.cs`, `NemotronProbeTests.cs` |
| Docs | `CLAUDE.md`, `harness/knowledge/_shared/agent-architecture.md` (신규) |

## 핵심 매핑 (실제 적용)

```
AgentReactorActor      → AgentLoopActor          ( /user/stage/bot/loop )
IAgentToolLoop         → IAgentLoop
AgentToolLoop          → LocalAgentLoop          (LLamaSharp + GBNF)
ExternalAgentToolLoop  → ExternalAgentLoop       (OpenAI-compat REST)
IAgentToolHost         → IAgentToolbelt
MockAgentToolHost      → MockAgentToolbelt
AgentToolSession       → AgentLoopRun            (Turns + FinalMessage + GuardStats)
AgentToolLoopOptions   → AgentLoopOptions
ToolLoopGuards         → AgentLoopGuards
ReactorBindings        → AgentLoopBindings       (ToolbeltFactory + OptionsFactory + AgentLoopFactory)
ReactorPhase           → AgentLoopPhase
ReactorProgress        → AgentLoopProgress
ReactorResult          → AgentLoopResult
ReactorToolCallInfo    → AgentLoopToolCallInfo
StartReactor           → StartAgentLoop
CancelReactor          → CancelAgentLoop
ResetReactorSession    → ResetAgentLoopMemory
SetReactorCallbacks    → SetAgentLoopCallbacks
```

유지된 이름 (의도적):
- `AgentBotActor` — UI 게이트웨이 역할이 정확
- `AgentToolGrammar` — GBNF 가 "tool" 호출을 실제 강제
- `ToolCall / ToolTurn / GuardStats` — 데이터 형태에 충실
- `LlmRuntimeSettings.AgentToolLoopMaxTokens` — JSON 영속화 키 보존 (사용자 settings.json 호환성)

## 검증

- ✅ `dotnet build Project/AgentZeroWpf/AgentZeroWpf.csproj -c Debug` — 0 errors, 7 warnings (모두 기존 warning, 본 미션 무관)
- 🔬 `dotnet test Project/ZeroCommon.Tests/ZeroCommon.Tests.csproj` — 실행 중. 결과는 commit 직전에 확인
- ⏭️ `dotnet test Project/AgentTest/AgentTest.csproj` — desktop session 의존, 본 미션 범위 밖 (후속 gate)

## 평가 (3축)

- **워크플로우 개선도: A** — 새 컨트리뷰터가 Anthropic *Building effective agents* / Claude Agent SDK 어휘를 그대로 매핑 가능. AgentBot(게이트웨이) ↔ AgentLoop(에이전트) 분리가 type 이름에 노출됨.
- **Claude 스킬 활용도: 4/5** — code-coach Mode 3 (Research consult) 로 진단 → operator 결정 → mission spec → 기계적 rename → 검증 게이트. tamer/build-doctor 까지는 동원하지 않음.
- **하네스 성숙도: L5 유지** — 새 어휘를 knowledge note 로 codify 했으므로 미래 PR 의 drift 방지 수단까지 함께 들어감 (M0011/M0012 의 nodejs-bash-pitfalls catalogue 와 동일 격).

## 다음 단계 제안

1. 다음 wrapper / actor PR 에서 `AgentReactor` / `ToolLoop` / `Host` 가 다시 등장하면 rename 회귀로 간주 — code-coach Mode 2 가 자동 검출.
2. `AgentTest` (desktop session 의존) 도 다음 GUI 작업 사이클에서 같이 돌려 Akka 경로 회귀 확인.
3. Multi-agent 확장이 실제로 필요해지면 (예: planner-agent, security-agent 같은 도메인 별 분리) Option C 로 한 단계 더 — `AgentBotActor` → `BotGatewayActor`, 그리고 `AgentLoopActor` → `AgentActor`.
