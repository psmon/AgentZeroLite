---
mission: M0040
title: Avalonia v2 ⑧ — 패키징/CI (win-x64 zip·osx-arm64 .app·macOS 스모크)
operator: psmon
language: ko
dispatched_to: [tamer, code-coach, test-runner]
status: done
started: 2026-09-19T03:30:00+09:00
finished: 2026-09-19T03:55:00+09:00
artifacts:
  - .github/workflows/avalonia-build.yml
  - Project/AgentZeroAvalonia/macos/{build-app.sh,Info.plist,entitlements.plist}
  - Project/AgentZeroAvalonia/Services/PtyHostFactory.cs (self-test 자식 linger)
  - Project/ZeroCommon.Tests/TerminalSeamTests.cs (폴링 대기)
  - Docs/avalonia-v2/macos-smoke.md, harness/knowledge/_shared/code-signing.md (§7 macOS)
  - CLAUDE.md (크로스플랫폼 호스트 절)
related: [M0039]
---

# M0040 수행결과 — 패키징/CI

## 수행 요약

계획 Phase 8. `avalonia-build.yml` 두 잡을 완성했다 — A(windows-latest): 복원·빌드·ZeroCommon 테스트·호스트 테스트·`selftest all`·
win-x64 self-contained publish·zip 아티팩트; B(macos-14): osx-arm64 빌드·ZeroCommon 테스트(Windows 경로 픽스처 3개 제외)·호스트 테스트·
publish·`-cli version`/`selftest all`/`AgentZeroLite.sh version`·`macos/build-app.sh`로 `.app`(Info.plist·엔타이틀먼트·ad-hoc codesign)·
번들 안의 래퍼 실행·`ditto` zip 아티팩트. `release.yml`은 무변경.

## 결과

### CI (run 35370508727, feat/avalonia-v2 @ 7387b7b)

| 잡 | 결과 |
|---|---|
| A · windows-latest | 성공 — ZeroCommon 860 통과 / 호스트 테스트 42 / selftest ipc·secrets(DPAPI)·pty(ConPTY) / publish + `AgentZeroLite-Avalonia-v0.25.0-win-x64.zip` |
| B · macos-14 | 성공 — ZeroCommon 807 통과 / 호스트 테스트 42(Porta.Pty 에코·종료 포함) / `-cli version` v0.25.0 / selftest ipc·secrets(**AES-GCM**)·pty(**forkpty**) / `AgentZeroLite.sh version` / `.app` ad-hoc 서명·검증 / 번들 내 래퍼 실행 / `AgentZeroLite-Avalonia-v0.25.0-osx-arm64.zip` |

### 잡은 것

1. **CI 빨강 원인 3건** — (a) M0033 세션 테스트가 청크 쓰기를 고정 650 ms만 기다려 느린 러너에서 첫 청크만 봄 → 최대 10 s 폴링.
   (b) 기존 ZeroCommon 픽스처 `Mp3ScannerTests.ToRelativePath`·`SshCommandBuilderTests`·`WhisperModelStoreTests`가 `C:\` 경로·`pwsh.exe`를
   전제해 macOS에서 실패 → macOS 잡에서 제외(후속: OS 인식으로 고치기). (c) macOS `selftest pty`: `sh -c echo`가 리더 스레드가 붙기 전에
   끝나 pty 마스터의 미읽은 출력이 버려짐(스폰·종료 코드 0은 확인, 마커만 없음) → 자식이 1 s linger. 실제 터미널은 장수 셸이라 무관.
2. Windows 잡은 첫 초록에서 이미 전 단계 통과 — 호스트 테스트·selftest·publish 모두 CI에서 재현됨.

## 평가

Acceptance: CI 잡 A/B 초록 ✓, `macos-smoke.md` 체크리스트 작성 ✓(실행은 운영자 Mac 필요 — 표 비어 있음), `code-signing.md` §7 macOS
서명·공증 절차 ✓, (stretch) macOS 로컬 LLM 조사는 미착수(1차 범위 밖, External만).

## 비고 — 1차(M0033–M0040) 전체 마감 상태

- 8 미션 모두 done. 브랜치 `feat/avalonia-v2`(워크트리 `AgentZeroLite-avalonia-v2`), WPF 프로젝트 무변경(매 미션 diff 0).
- ZeroCommon 변경은 추가/울타리 + 버그 수정 2건(`PrependPath` 키 선택, Gemma 네이티브 툴콜 변환) + 테스트 폴링 1건 — 정책대로 main에 먼저 PR.
- 운영자 스모크 항목은 각 미션 기록의 "평가" 절에 있다(Windows GUI 손 조작, macOS 전체).
